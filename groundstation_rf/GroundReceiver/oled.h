// ----------------------------------------------------------------------------
// ----------------------------------------------------------------------------
#ifndef __OLED_H__
// ----------------------------------------------------------------------------
#define __OLED_H__
// ----------------------------------------------------------------------------
#include <stdint.h>
#include "heltec.h"
#include "GroundReceiver.h"  // for top-level defines 
// ----------------------------------------------------------------------------
#define DISPLAY_PERIOD_MS       500     // max OLED framerate 2Hz
// ----------------------------------------------------------------------------
extern uint8_t  g_dbgstr_lit; 
extern uint32_t g_tm_dbgstr_off;
extern char g_dbg_str0[32];
extern char g_dbg_str1[32];
// ----------------------------------------------------------------------------
void oledInit();
void oledRedraw();

#if DEBUG_LOGGING
	void _dbgStr0(char * buf);
	void _dbgStr1(char * buf);
	void _dbgHex0(uint8_t * buf, uint16_t ct);
	void _dbgHex1(uint8_t * buf, uint16_t ct);
#else
	#define _dbgStr0(...)
	#define _dbgStr1(...)
	#define _dbgHex0(...)
	#define _dbgHex1(...)
#endif
// ----------------------------------------------------------------------------
#endif
// ----------------------------------------------------------------------------
// ----------------------------------------------------------------------------
